{
  "name": "toggle-windows",
  "version": "1.0.0",
  "description": "Weather station data to tell me when to open and close the windows depending on outside vs inside temp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ambient-weather-api": "0.0.6",
    "twilio": "^3.44.0"
  }
}
